""""""""""""""
" Author Rick Poot ( rlpoot@gmail.com )
" Modified: 27 october 2010
""""""""""""""

" Use vim setting rather that vi settings
set nocompatible

" Enable syntax highlighting
syntax on

" Automaticly ident when adding a curly bracket, etc.
set smartindent

" Tabs should be converted to a group pof 4 spaces
set shiftwidth=4
set tabstop=4
set expandtab
set smarttab

" Minimal number of screen lines to keep above and below the cursor
"set scrolloff=999

"Use UTF-8
set encoding=utf-8

" Set color scheme that i like
colorscheme desert

" Show line number, cursor position.
set ruler

" Display incomplete command
set showcmd

" Search as you type
set incsearch

" Ignore case when searching
set ignorecase

set nu

set bs=2
